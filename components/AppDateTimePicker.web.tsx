import React from 'react';

export type DateTimePickerProps = {
  value: Date;
  mode?: string;
  onChange?: (...args: any[]) => void;
};

export default function AppDateTimePicker(_props: DateTimePickerProps) {
  return null;
}
